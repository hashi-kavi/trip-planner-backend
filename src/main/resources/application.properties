spring.application.name=budgettrip

# Database connection
spring.datasource.url=jdbc:mysql://localhost:3307/budgettrip
spring.datasource.username=root
spring.datasource.password=hashi2014kavi@

# JPA/Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Increase multipart file size limits
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Alternatively, for Spring Boot 2.0+ you might need:
spring.servlet.multipart.maxFileSize=10MB
spring.servlet.multipart.maxRequestSize=10MB

spring.jackson.serialization.fail-on-empty-beans=false
spring.jackson.serialization.write-dates-as-timestamps=false
